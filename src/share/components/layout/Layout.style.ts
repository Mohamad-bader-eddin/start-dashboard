import styled from "styled-components";
import { NAVBAR_HIGHT } from "../../constants";

export const StyledLayout = styled.div`
  margin-top: ${() => `${NAVBAR_HIGHT}px`};
  position: relative;
  padding: 15px;
`;
